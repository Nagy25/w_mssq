name: w_mssq
description: A  Flutter windows plugin for connect sql server with odbc driver.
version: 0.0.1
homepage:

environment:
  sdk: '>=2.19.6 <3.0.0'
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1



flutter:

  plugin:
    platforms:
      windows:
        pluginClass: WMssqPluginCApi
